Description:
  Creates a Full-Stack AngularJS + Node app

Example:
  yo macend

Sub Generators:

  Server Side:
    macend:endpoint

  Client Side:
    macend:route
    macend:controller
    macend:filter
    macend:directive
    macend:service
    macend:provider
    macend:factory
    macend:decorator

  Deployment:
    macend:openshift
    macend:heroku
